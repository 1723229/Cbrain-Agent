{
  "mcpServers": {
    "cbrain-agent": {
      "type": "stdio",
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/scripts/mcp-bridge.mjs"],
      "cwd": "${CLAUDE_PLUGIN_ROOT}"
    }
  }
}
