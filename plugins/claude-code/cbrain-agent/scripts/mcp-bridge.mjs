#!/usr/bin/env node
import { createInterface } from "node:readline";import { loadGatewayConfig } from "./gateway-config.mjs";import { ensureEventRelay } from "./event-relay.mjs";
const config=await loadGatewayConfig({optional:true});let sessionId="",protocolVersion="";const lines=createInterface({input:process.stdin,crlfDelay:Infinity});if(config)void ensureEventRelay(config).catch(()=>false);
for await(const line of lines){if(!line.trim())continue;let message;try{message=JSON.parse(line);if(message?.method==="initialize")protocolVersion=message.params?.protocolVersion||"";if(config)await forward(message);else dormant(message)}catch(error){writeError(message?.id??null,error)}}
async function forward(message){const headers={Accept:"application/json, text/event-stream",Authorization:`Bearer ${config.apiKey}`,"Content-Type":"application/json"};if(sessionId)headers["Mcp-Session-Id"]=sessionId;if(protocolVersion)headers["MCP-Protocol-Version"]=protocolVersion;const response=await fetch(config.mcpUrl,{method:"POST",headers,body:JSON.stringify(message),signal:AbortSignal.timeout(30000)});if(!response.ok)throw new Error(`gateway HTTP ${response.status}`);sessionId=response.headers.get("mcp-session-id")||sessionId;if(response.status===202||!response.body)return;const contentType=response.headers.get("content-type")||"",body=await response.text();if(contentType.includes("text/event-stream")){for(const payload of parseSse(body))process.stdout.write(`${payload}\n`)}else if(body.trim())process.stdout.write(`${body.trim()}\n`)}
function dormant(message){if(message?.id===undefined)return;let result={};if(message.method==="initialize")result={protocolVersion:protocolVersion||"2025-03-26",capabilities:{tools:{}},serverInfo:{name:"cbrain-agent-unconfigured",version:"0.2.0"}};else if(message.method==="tools/list")result={tools:[]};else if(message.method==="ping")result={};else return writeError(message.id,new Error("Agent Memory is not configured for this workspace"));process.stdout.write(`${JSON.stringify({jsonrpc:"2.0",id:message.id,result})}\n`)}
export function parseSse(body){const messages=[];for(const event of body.split(/\r?\n\r?\n/)){const data=event.split(/\r?\n/).filter((line)=>line.startsWith("data:")).map((line)=>line.slice(5).trimStart()).join("\n");if(data)messages.push(data)}return messages}
function writeError(id,error){const message=error instanceof Error?error.message:String(error);process.stdout.write(`${JSON.stringify({jsonrpc:"2.0",id,error:{code:-32603,message}})}\n`)}
