import assert from "node:assert/strict";
import { createServer } from "node:http";
import { mkdtemp, readFile, rm, writeFile } from "node:fs/promises";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { spawn } from "node:child_process";
import { fileURLToPath } from "node:url";
import test from "node:test";
import { startEventRelay } from "./event-relay.mjs";

test("Claude hooks fail open when local configuration is malformed",async()=>{
  const directory=await mkdtemp(join(tmpdir(),"hiper-claude-bad-config-")),config=join(directory,"config.json");await writeFile(config,"not-json");
  try{const result=await runHook(config,"user-prompt",{session_id:"bad",cwd:directory,prompt:"hello",hook_event_name:"UserPromptSubmit"});assert.equal(result.code,0);assert.equal(result.stdout.trim(),"{}");assert.equal(result.stderr,"")}finally{await rm(directory,{recursive:true,force:true})}
});

test("an unbound Claude workspace requests explicit Team and Agent confirmation",async()=>{
  const directory=await mkdtemp(join(tmpdir(),"hiper-claude-bind-")),config=join(directory,"config.json"),state=join(directory,"state.json");
  const server=createServer((request,response)=>{request.resume();request.on("end",()=>{response.writeHead(200,{"Content-Type":"application/json"});response.end(JSON.stringify({status:"unbound",binding_request_id:"bind-1",teams:[{team_id:"team-a",name:"Team A"}],agents:[{agent_id:"agent-a",team_id:"team-a",name:"Claude Agent"}]}))})});
  await new Promise(resolve=>server.listen(0,"127.0.0.1",resolve));await writeFile(config,JSON.stringify({gatewayUrl:`http://127.0.0.1:${server.address().port}`,token:"test"}));
  try{const result=await runHook(config,"user-prompt",{session_id:"session-1",cwd:directory,prompt:"fix the build",hook_event_name:"UserPromptSubmit"},{HIPER_AGENT_MEMORY_STATE:state,HIPER_WORKSPACE_KEY:"test:claude-bind"});assert.equal(result.code,0);const output=JSON.parse(result.stdout);assert.equal(output.hookSpecificOutput.hookEventName,"UserPromptSubmit");assert.match(output.hookSpecificOutput.additionalContext,/Team A/);assert.match(output.hookSpecificOutput.additionalContext,/explicit confirmation/);assert.match(output.hookSpecificOutput.additionalContext,/fix the build/)}finally{server.close();await rm(directory,{recursive:true,force:true})}
});

test("Claude lifecycle uses recall synchronously and writes tool, stop, and end asynchronously",async()=>{
  const directory=await mkdtemp(join(tmpdir(),"hiper-claude-life-")),bridges=join(directory,"bridges"),config=join(directory,"config.json"),state=join(directory,"state.json"),requests=[];
  const server=createServer((request,response)=>{let body="";request.on("data",chunk=>body+=chunk);request.on("end",()=>{requests.push({url:request.url,body:JSON.parse(body||"{}")});if(request.url==="/v1/workspaces/resolve"){response.writeHead(200,{"Content-Type":"application/json"});response.end(JSON.stringify({status:"bound",team_id:"team",agent_id:"agent",agent_name:"Claude Agent",context_id:"ctx-1",additionalContext:"profile"}))}else if(request.url==="/v1/hooks/prompt"){response.writeHead(200,{"Content-Type":"application/json"});response.end(JSON.stringify({additionalContext:"recalled decision"}))}else{response.writeHead(202,{"Content-Type":"application/json"});response.end(JSON.stringify({accepted:1}))}})});
  await new Promise(resolve=>server.listen(0,"127.0.0.1",resolve));const gatewayUrl=`http://127.0.0.1:${server.address().port}`;await writeFile(config,JSON.stringify({gatewayUrl,token:"test"}));const relay=await startEventRelay({gatewayUrl,token:"test"},{directory:bridges,batchWindowMs:20});const env={HIPER_AGENT_MEMORY_STATE:state,HIPER_AGENT_MEMORY_BRIDGE_DIR:bridges,HIPER_WORKSPACE_KEY:"test:claude-life"};
  try{await runHook(config,"session-start",{session_id:"session-2",cwd:directory,hook_event_name:"SessionStart"},env);const prompt=await runHook(config,"user-prompt",{session_id:"session-2",cwd:directory,prompt:"implement it",hook_event_name:"UserPromptSubmit"},env);assert.match(JSON.parse(prompt.stdout).hookSpecificOutput.additionalContext,/recalled decision/);await runHook(config,"tool-use",{session_id:"session-2",cwd:directory,tool_use_id:"toolu-1",tool_name:"Bash",tool_input:{command:"pnpm test"},tool_response:{ok:true}},env);await runHook(config,"stop",{session_id:"session-2",cwd:directory,last_assistant_message:"done",hook_event_name:"Stop"},env);await runHook(config,"session-end",{session_id:"session-2",cwd:directory,reason:"exit",hook_event_name:"SessionEnd"},env);await waitFor(()=>requests.filter(item=>item.url==="/v1/hooks/batch").flatMap(item=>item.body.events).length===3);const events=requests.filter(item=>item.url==="/v1/hooks/batch").flatMap(item=>item.body.events);assert.deepEqual(events.map(item=>item.type),["tool_use","stop","session_end"]);assert.equal(events[0].body.context_id,"ctx-1");assert.notEqual(events[1].body.turn_id,"current");assert.equal(Object.keys(JSON.parse(await readFile(state,"utf8"))).length,0)}finally{await relay.close();server.close();await rm(directory,{recursive:true,force:true})}
});

test("Claude consumes a scoped workspace_bind MCP result without resolving twice",async()=>{
  const directory=await mkdtemp(join(tmpdir(),"hiper-claude-bound-result-")),config=join(directory,"config.json"),state=join(directory,"state.json"),key=`claude-code:session-3:${directory}`,requests=[];
  const server=createServer((request,response)=>{let body="";request.on("data",chunk=>body+=chunk);request.on("end",()=>{requests.push({url:request.url,body:JSON.parse(body||"{}")});response.writeHead(request.url==="/v1/hooks/tool-use"?202:200,{"Content-Type":"application/json"});response.end(JSON.stringify(request.url==="/v1/hooks/prompt"?{additionalContext:"bound recall"}:{accepted:1}))})});
  await new Promise(resolve=>server.listen(0,"127.0.0.1",resolve));await writeFile(config,JSON.stringify({gatewayUrl:`http://127.0.0.1:${server.address().port}`,token:"test"}));await writeFile(state,JSON.stringify({[key]:{bindingRequestId:"bind-3",bindingExpiresAt:Date.now()+60000,bindingOptions:{teams:[],agents:[]},pendingOriginalPrompt:"implement it",turnId:"turn-3",updatedAt:Date.now()}}));const env={HIPER_AGENT_MEMORY_STATE:state,HIPER_WORKSPACE_KEY:"test:claude-result"};
  try{await runHook(config,"tool-use",{session_id:"session-3",cwd:directory,tool_use_id:"tool-bind",tool_name:"mcp__plugin_claude-code-agent-memory_hiper-agent-memory__workspace_bind",tool_response:{structuredContent:{status:"bound",context_id:"ctx-3",team_id:"team",agent_id:"agent",agent_name:"Claude Agent"}}},env);assert.equal(JSON.parse(await readFile(state,"utf8"))[key].contextId,"ctx-3");const prompt=await runHook(config,"user-prompt",{session_id:"session-3",cwd:directory,prompt:"continue",hook_event_name:"UserPromptSubmit"},env);assert.match(JSON.parse(prompt.stdout).hookSpecificOutput.additionalContext,/bound recall/);assert.equal(requests.some(item=>item.url==="/v1/workspaces/resolve"),false)}finally{server.close();await rm(directory,{recursive:true,force:true})}
});

function runHook(config,mode,input,extraEnv={}){return new Promise((resolve,reject)=>{const child=spawn(process.execPath,[fileURLToPath(new URL("./agent-memory-hook.mjs",import.meta.url)),"claude-code",mode],{env:{...process.env,HIPER_AGENT_MEMORY_CONFIG:config,HIPER_AGENT_GATEWAY_URL:"",HIPER_AGENT_GATEWAY_TOKEN:"",...extraEnv},stdio:["pipe","pipe","pipe"]});let stdout="",stderr="";child.stdout.on("data",chunk=>stdout+=chunk);child.stderr.on("data",chunk=>stderr+=chunk);child.on("error",reject);child.on("close",code=>resolve({code,stdout,stderr}));child.stdin.end(`${JSON.stringify(input)}\n`)})}
async function waitFor(predicate,timeoutMs=3000){const deadline=Date.now()+timeoutMs;while(Date.now()<deadline){if(predicate())return;await new Promise(resolve=>setTimeout(resolve,25))}assert.fail("timed out waiting for relay")}
